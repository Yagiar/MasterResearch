apiVersion: 1
providers:
  - name: "uavdet"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
# Дашборды (.json) добавляются сюда позже — минимальный provisioning для пилота.
