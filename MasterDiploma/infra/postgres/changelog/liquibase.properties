# Параметры Liquibase для контейнера `liquibase` (infra/docker-compose.yml).
# changelog-файл смонтирован в /liquibase/changelog; БД — сервис `postgres` в docker-сети.
changeLogFile=db.changelog-master.xml
url=jdbc:postgresql://postgres:5432/uavdet
username=uavdet
password=uavdet
driver=org.postgresql.Driver
searchPath=/liquibase/changelog
