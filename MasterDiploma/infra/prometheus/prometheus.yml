global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "uavdet-sink"
    static_configs:
      - targets: ["sink:9108"]
  # при необходимости добавить targets для других сервисов, экспонирующих /metrics
