"""fusion — движок мультимодального слияния и принятия решений.

Consumer-сервис: топик `inference` -> временное выравнивание детекций видео/аудио
в окне ±ε (per source_id) -> применение стратегии слияния -> публикация `DecisionMsg`
в топик `decisions`.

Стратегии (паттерн Strategy): `video-only` (MVP — решение по одному видеоканалу),
`audio-only`, `late` (взвешенная сумma p_v/p_a + правило компенсации Δ), `hybrid`.
Веса каналов задаёт `GatingPolicy` (на MVP — фиксированные).
"""
