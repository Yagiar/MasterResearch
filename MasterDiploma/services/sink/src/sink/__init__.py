"""sink — терминальный потребитель решений.

Consumer-сервис: топик `decisions` -> структурный лог + запись в jsonl (для офлайн-анализа)
+ агрегаты в Prometheus (`/metrics`). Опционально читает и `inference` (для подсчёта
e2e-latency «по факту» и числа детекций по модальностям).
"""
