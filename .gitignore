# Вложенные папки-репозитории: у каждой свой git и своя история (у MasterDiploma и
# MasterDiplomaVaultObsidian — remote на GitHub), в корневой репо они не включаются.
# Если их .git будут удалены — уберите строки, чтобы корневой репо трекал содержимое.
/MasterDiploma/
/MasterDiplomaVaultObsidian/
/NIR-2-SEM-Full/

# Вложенные git-репозитории внутри Диплом-бака (обнаружены 2026-09-04): клон
# Koldim2001/SORT-DeepSORT-Tracker с локальным коммитом «рабочий байтрек» и
# SendVideoByUdpOnIphone (Xcode-проект, локальный git без remote, исходники
# частично только в истории). До решения о судьбе их .git — игнорируем, как и выше.
/Диплом-бака/SORT-DeepSORT-Tracker/
/Диплом-бака/iphone_sender_video/SendVideoByUdpOnIphone/

# Мусор ОС и временные lock-файлы MS Office
.DS_Store
~$*

# Агрегированные логи/артефакты в корне, если появятся
*.log
