# ИТЕРАЦИЯ 27 — ГОСТ-подписи таблиц раздела 7 + найден и исправлен дефект рендера

- **Дата:** 2026-09-04
- **Цель:** нумерация таблиц раздела 7 по ГОСТ 7.32-2017 (подпись над таблицей, «Таблица 7.N — …») + контроль рендера.
- **Правки:** `otchet.md` (+4 подписи +4 пустые строки), `otchet_clean.md` (перегенерирован), `otchet.docx`/`otchet_draft.docx` (пересобраны, в git не входят).

## Сделано

Подписи (в отчёте до этого подписей у таблиц не было ни в одном разделе — раздел 7 задаёт стиль):

- Таблица 7.1 — Системные показатели сквозных прогонов №1–2 (CPU против GPU)
- Таблица 7.2 — Режимы слияния при неработающей акустике (прогон №3)
- Таблица 7.3 — Режимы слияния с работающей акустикой AST (прогон №4)
- Таблица 7.4 — Точность/полнота/F1 политик слияния против GT «БПЛА активен»

(Таблиц в разделе 7 — четыре; метрики детекторов в 7.2 даны инлайн-текстом.)

## ⚠️ Найден и исправлен дефект рендера (важно для всех будущих правок)

После вставки подписей **pandoc молча выронил все 4 таблицы раздела 7** из .docx: подпись, идущая **вплотную** к строкам таблицы (без пустой строки), не даёт pandoc распознать pipe-table — таблица склеивается с абзацем подписи. Дефект обнаружен только подсчётом `<w:tbl>` в распакованном document.xml (стало 4 вместо 8). Контент-проверки по строкам при этом «зелёные» — текст таблиц попадал в docx как абзац.

**Правило:** подпись таблицы отделяется от таблицы пустой строкой. После КАЖДОЙ правки раздела с таблицами — контрольный подсчёт `<w:tbl>` (ожидается 8: 4 в разделах 1–6 + 4 в разделе 7).

## Итоговое состояние

| Проверка | otchet.docx | otchet_draft.docx |
|---|---|---|
| `<w:tbl>` | **8** ✓ | **8** ✓ |
| ГОСТ-подписи 7.1–7.4 | 4/4 ✓ | 4/4 ✓ |
| Источники [37]–[39], раздел 7.1–7.8 | ✓ | ✓ |

## Осталось за автором (вне скоупа итерации)

- Подписи таблиц разделов 1–6 (в разделах 2/4/5 есть таблицы без подписей — добавить «Таблица 2.1 — …» по аналогии; ~4 таблицы).
- ГОСТ-финализация .docx (поля/шрифты/титульник — template.docx кафедры).
