# ИТЕРАЦИЯ 20 — Поправки в Vault-заметку «Этап 6» по итогам цикла

- **Дата:** 2026-09-04
- **Цель:** перенести в каноничную заметку этапа ([[Этап 6 — Пилотные исследования (baseline + пробный fusion)]]) три поправки, найденные циклом, — чтобы отчёт по НИР писался уже из исправленной базы.
- **Правки (3 блока):**
  1. Интерпретация прогона №4, п.2 — вставка-поправка: «пустые» 0–11/66–73 с визуально не пусты (дрон на полу виден все 73 с), двухслойный GT `visible`/`airborne`, честные числа it-03 (лучший для `airborne` — аудио 0.920; fusion-выигрыш — с медианой, 0.913–0.922).
  2. Интерпретация №3 — пометка «опровергнуто данными» для gating по энтропии выхода (it-03/it-06: H(0)=0, F1 = plain late; +0.8 п.п. — в пределах шума) со ссылками на внедрённые замены (`audio_temporal_k`, `audio_health_gate`).
  3. Открытые вопросы: GT-разметка отмечена выполненной (it-02); entropy-пункт заменён на опровергнутый + внедрённые альтернативы; добавлены новые открытые пункты — аномалия доставки аудио в №4 (7% против 75% смешанных окон) и повторный ablation с `run_id`/ротацией jsonl; MMAUD-пункт получил ссылку на план it-17.
  4. Новый раздел «Поправки цикла 2026-09-04» со сводкой и wikilinks на три сводные заметки цикла.

## Принцип

Исходные формулировки прогона сохранены — поправки добавлены блоками-цитатами с датой и ссылкой на артефакт (`research/it-XX`), чтобы история рассуждений этапа осталась видимой (журнал не переписывается задним числом, а корректируется явно).

## Статус

Заметка готова как основа раздела 7 отчёта НИР-2; коммит вместе с research/.
