# ИТЕРАЦИЯ 78 — Семейство обобщённого среднего над парой conf (p-сетка): закрытие измерения «функция голосования» (🟢 ЗАВЕРШЕНА 2026-09-21, D1 КРАСНЫЙ)

- **Дата:** 2026-09-21. **Статус:** 🟢 завершена (ноль инференса — существующие CSV).
- **Триггер:** урок LESSONS от 21.09 («исчерпано над X, а не „исчерпано“»): it-74 измерил
  ДВЕ крайние точки функции голосования — AND=min (p→−∞) и OR=max (p→+∞) — и ничего между
  ними. Между тем AND@0,4 (94,7 % / 7,2 %) — это компромисс на лобовом отрезке между new@0,4
  (94,8 % / 10,2 %) и точкой жёсткого пересечения; мягкое пересечение (обобщённое среднее) —
  незанятая внутренность пространства, и именно её отсутствие в переборе делает вывод
  «голосование изучено» пока неверифицированным.

## Конструкция (фиксируется ДО чисел)

- Ряд: `M_p(c_old, c_new) := ((c_old^p + c_new^p)/2)^(1/p)` — обобщённое среднее;
  предельные точки сетки: p→−∞ = AND (минимум), p→+∞ = OR (максимум).
- Сетка: p ∈ {−8, −4, −2, −1, −0,5, 0,25 (=геометрическое), 0,5, 1 (арифметическое), 2, 4, 8}
  × τ ∈ {0,30; 0,35; 0,40; 0,45; 0,50} (τ — шаг сетки it-70). Числовые значения M_p
  считаются в loge-пространстве (устойчиво к conf=0: нулевой член ⇒ M_p=0 при p>0 — совпадает
  с AND-семантикой; при p<0 нуль доминирует корректно через exp(logaddexp(−∞))).
- Материал и метрики — КАНОНИЧЕСКИЕ ряды it-74 (без новых прогонов):
  полёт/дальн: `mmaud_sahi_full.csv` ⋈ `mmaud_sahi_full_new.csv` (5091 кадр, GT-высота z>1,
  дальн 10–19 м); FP: `coco_bg_fp_it69v1-{old,new}.csv` (400 независимых фонов, доля кадров
  с M_p ≥ τ); контур (для выбранной точки): каркас it-71/75, pv := M_p-ряд, гейт 0,25, D0.

## Предрегистрированные критерии

- **D1 (доминирование):** существует (p, τ) с полёт-реколлом ≥ 94,7 % И FP ≤ 7,2 % И
    хотя бы одной строгой компонентой (реколл ≥ 95,5 % ИЛИ FP ≤ 6,0 %) — тогда эта точка
    регистрируется НОВЫМ кандидатом image-линейки вместо AND@0,40.
- **D2 (Парето-карта):** для каждой τ печать границы реколл×FP по p — отчётный артефакт,
    не блокатор.
- **D3 (контур, только если D1 зелёный):** M_p-ряд в каркасе it-75: K1 R ≥ 0,98 ∧
    K2 F1 ≥ 0,95 ∧ K3 FP ≤ 8. D1 красный ⇒ контур не считается (нет предмета).
- **Правило вердикта:** D1 красный ⇒ измерение «функция голосования» закрывается ЧЕСТНЫМ
    отрицанием: сетка из 55 внутренних точек + 2 границы не улучшает frontier AND@0,40 —
    вывод it-74 распространяется на всё семейство средних (рамка «исчерпано над p-семейством»).
    D1 зелёный ⇒ пересмотр кандидата (image=M_p; контур — по D3).

## Ограничения (заранее)

1. Тот же материал, что it-74 (конвенция цикла): подтверждения независимостью нет; живой A/B
   по-прежнему вне (решение автора о ×2).
2. Цена доставки не меняется: 2 инференса/кадр.
3. Дальний бакет и интервальные запасы — справочно; при зелёном D1 бутстрап новой точки —
   отдельным дополнением (как it-74), критерии не меняются.
4. Сетка p дискретна; непрерывная оптимизация (p, τ) по тому же материалу — переобучение
   метрики на оценочном наборе, сознательно НЕ делается.

## Артефакты

`research/it78_gmean_vote.py` → `research/it78_gmean_vote.{csv,txt}` (сетка + вердикт-строка).
Запуск после коммита предрегистрации.

## Результаты (2026-09-21)

**Честная заметка о процессе.** Первый прогон дал карту с насыщением строк p<0
(100,0 / 70,2 при τ=0,30) — в реализации `gmean` стояла неверная log-форма
(`exp(logaddexp(lx,ly)/p − ln2/p)` = ((x+y)/2)^(1/p) вместо M_p). Вердикт из того
запуска **не выносился и не записывался**; ловушка вскрылась самими же предрегистрированными
проверками (крайние точки AND/OR не совпали с it-74) и анализом карты. Фикс: правильная
log-форма с max-скейлингом при p>0 и min-скейлингом при p<0 (стабильно при p→±1e9) плюс
`_selftest_gmean()` (гармоника p=−1, арифметика p=+1, пределы p→±∞ → min/max) — вызывается
при импорте модуля. Итоговый прогон ниже — после фикса.

**Сами-проверки (блокатор интерпретации): зелёные.** AND@0,40: rec=94,7 / FP=7,2 — OK
(совпало с it-74); OR@0,35: rec=100,0 / FP=49,8 — OK. Join-рядов 5091 кадров, фон 400 —
по канону it-74.

**D2 — Парето-карта (реколл полёта %, FP % в скобках; полная таблица в `.txt`):**

| p | τ=0,30 | τ=0,35 | τ=0,40 | τ=0,45 | τ=0,50 |
|---|---|---|---|---|---|
| AND | 98,7 (11,5) | 97,1 (9,2) | **94,7 (7,2)** | 90,2 (6,0) | 82,7 (5,2) |
| −8 | 99,1 (13,0) | 98,1 (10,0) | 96,2 (7,5) | 93,2 (6,5) | 86,9 (5,2) |
| −2 | 99,5 (14,0) | 99,0 (12,0) | 97,4 (9,8) | 94,6 (6,8) | 88,7 (5,5) |
| −0,5 | 99,7 (15,2) | 99,2 (13,0) | 98,1 (10,8) | 95,3 (7,0) | 89,2 (5,5) |
| 0,25 | 99,8 (18,0) | 99,4 (14,0) | 98,4 (11,5) | 95,6 (7,8) | 89,7 (5,5) |
| 1 | 99,9 (27,5) | 99,6 (20,2) | 98,7 (13,5) | 96,2 (9,0) | 90,3 (6,2) |
| 4 | 100,0 (50,0) | 99,9 (41,8) | 99,3 (33,5) | 97,5 (24,8) | 91,7 (16,0) |
| OR | 100,0 (59,5) | 100,0 (49,8) | 99,8 (42,8) | 99,1 (36,0) | 96,2 (28,7) |

**D1 — КРАСНЫЙ.** Ни одна из 55 точек сетки не доминирует AND@0,40
(rec ≥ 94,7 ∧ FP ≤ 7,2 ∧ [rec ≥ 95,5 ∨ FP ≤ 6,0]) — кандидатов нет. Структура карты:
внутренние p монотонно перемещаются вдоль Парето-фронта между AND и OR, увеличивая реколл
ценой FP; у AND-конца фронт жёсткий — сосед p=−8 при τ=0,40 даёт 96,2/7,5 (FP хуже),
при τ=0,45 — 93,2/6,5 (реколл хуже). D3 (контур) не считался по предрегистрированному
правилу: нет предмета.

**Вердикт цикла (правило из предрегистрации):** измерение «функция голосования» закрыто
честным отрицанием — вывод it-74 о том, что AND@0,4 — лучшая доступная точка image-фронтера,
распространяется на всё семейство обобщённых средних (рамка: «исчерпано над p-семейством
средних», НЕ «исчерпано вообще»). Профиль ансамбля не меняется: image = AND@0,40
(94,7 / 7,2), контур = OR (R 1,000 / F1 0,965 / FP 8), mAP-ось красная для merge (it-77).

**Ограничения подтверждены как в предрегистрации:** тот же материал (независимости нет);
дискретность сетки сознательна (непрерывная подстройка (p,τ) = переобучение метрики).

