# ИТЕРАЦИЯ 38 — Фикс по ревью: снятие сверх-утверждений, атрибуция рецепта, правка документов (§2/§3/§9/§11)

- **Дата:** 2026-09-05
- **Источник:** внешнее ревью GPT-6-Astra (`review-from-gpt-6-astra/MasterResearch_review.md`, пп. 2, 3, 9, 11; вторая версия пп. 2, 4, 11).
- **Компоненты:** `research/iterations/it-06/it-08/it-12` (датированные дополнения), `MasterDiploma/reports/НИР-2/otchet.md` (разделы 7.7/7.8 — поправка с таблицей 7.5), сам ревью-пакет закоммичен как источник работ.

## Исправленные формулировки

1. **it-06** («статистически значимо», «ни одна политика не превосходит одиночную», «entropy-gating опровергнут»): датированное дополнение — первые два сняты по it-36 (артефакт обнуления p_drone; теста значимости не было), третье смягчено («одна эвристика на искажённых вероятностях», после честного p_drone entropy = 0.961).
2. **it-08** («каузальная медиана без добавленной задержки»): дополнение — задержка обнаружения ~k/2 шагов сетки (~1 с при k=5, hop 0.5 с); отсутствие заглядывания в будущее ≠ нулевая задержка. Дублирует фикс в docstring `fusion/temporal.py` (it-31).
3. **it-12** («модель бимодальна», «калибровка порога опровергнута»): дополнение — бимодальность {0}∪[0.5;1] была артефактом кода; повторить калибровку на честных вероятностях.
4. **otchet.md §7.7**: к прежним выводам применены зачёркивания с обоснованием; добавлена «Поправка 2026-09-05» с таблицей 7.5 (честные вероятности, симметричные фильтры: late+кауз.медиана-5 = 0.957 vs audio-only 0.913) и скорректированными выводами (вклад fusion +4.4 п.п.; Δ-правило вредит; entropy снято с «опровержения»; задержка медианы ~1 с; оговорки единственного клипа и development-уровня). §7.8 — обновлена ссылка на прирост (+4.4 п.п. вместо +5.4).
5. **Атрибуция рецепта** (ревью §9/§11): в поправке §7.7 зафиксировано, что окна 1 с/шаг 0.5 с + медианная фильтрация вероятностей соответствуют рекомендациям карточки модели `Rashidbm/samid-drone-detector` и подаются как адаптация рекомендованной обработки, а не новый метод.
6. **HybridFusion naming** (ревью §9): уточнён в коде (docstring `hybrid.py`, it-32: «нелинейная комбинация решений, не слияние признаков») и в it-32/otchet терминологии.
7. **Целевое событие** (ревью §3, P0 «зафиксировать целевое событие»): двухуровневый GT (visible/airborne) уже описан в §7.7; в поправке явно указано, что все числа 7.7 относятся к `airborne`, а слияние модальностей смешивает разные события (виден стоящий дрон / слышны винты) — интерпретация чисел только в рамках этой оговорки; полная переразметка (присутствие/видимость/винты/полёт отдельными полями) — в план (P1).

## Коммит ревью-пакета

`review-from-gpt-6-astra/` (ревью, `reviewer_checks.py`, `reviewer_checks_results.json`) закоммичен как источник итераций it-31…38: пять контрпримеров рецензента закрыты тестами (it-31: дубли аудио; it-32: нулевой вес; it-33: порядок доставки; it-35: подгонка фазы/mixed; it-36: audio-only fallback).

## Ограничения

- ~~otchet.docx не перегенерирован~~ — docx пересобран pypandoc 3.9 в этой же итерации (таблица 7.5 и поправка на месте).
- Статистическая часть ревью (§4/§10: независимые сессии, интервалы с группировкой по записям) кодом не решается — это сбор новых данных (P1, MMAUD/сессии); зафиксировано в выводах как оговорка.
