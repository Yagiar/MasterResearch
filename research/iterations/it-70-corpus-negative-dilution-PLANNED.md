# it-70 — Рычаг рецепта корпуса: разбавление фоновых негативов (вариант A)

**Статус: 🟡 запущена (предрегистрация 2026-09-21, до любых прогоновых метрик it-70).**
План. Факты и вердикты — в этом файле по мере работ; финал → `it-70-….md` (rename), строка в `INDEX.md`, манифест.

## Предистория (наследование)

- it-65: дообучение YOLOv8s на корпусе DUT+COCO-фоны (негативы 1,26 % train / 3,3 % val / 1,8 % test)
  убило FP (V1 it-69: @0,25 61,8 %→17,8 %; @0,5 27,5 %→6,5 %) и дало E5-красный:
  SAHI-recall полёта MMAUD @0,5 = 84,3 % < 89,5 %.
- it-69 (шаг 0 + V1/V2/V3): механизм E5 — сдвиг conf-распределения новых весов вниз на ~0,1
  (пересчёт τ-сетки из per-frame CSV: τ=0,4 → 94,8 %; τ=0,25 → 99,4 %). Перекалибровка
  операционной точки офлайн подтверждена (V1/V2 зелёные), но в контуре не проходит (V3 красный:
  боевой гейт уже 0,25; потери airborne-окон — оконная логика fusion, не порог). Вывод it-69:
  «починка — на уровне рецепта корпуса».
- Хвост it-65 (бэклог, дословно): «снизить долю фоновых негативов (1,3 % → ~0,3–0,5 %) …
  Проверять той же вердикт-машинкой E1–E5 с новой предрегистрацией порогов».

## Гипотеза (формулировка)

Мы claim, что **снижение доли фоновых негативов COCO в обучении с 1,26 % до ~0,4 %**
(300 из 900 кадров, всё остальное — бит-в-бит корпус и гиперпараметры it-65)
поднимает SAHI-recall полёта MMAUD @0,5 новых весов до ≥ 89,5 % (E5),
сохраняя FP-подавление на независимых фонах (E1) и метрики домена (E2–E4),
против базы: старые майские веса (E5 = 94,5 %, FP@0,5 = 27,5 %) и веса it-65
(E5 = 84,3 %, FP@0,5 = 6,5 %). Job новой модели — вернуть recall, не потеряв FP.

Варианты B (distant-негативы из MMAUD) и C (двухстадийная доводка) — вне it-70
(одна изменённая вещь за раз; регистрация при красном A).

## Механизм изменения (единственный рычаг)

1. Корпус `_prepared/visual` it-65 детерминирован (`RANDOM_SEED=1337`, команды в `REPRODUCE.md`);
   перетирать prepare-visual НЕ будем — текущее состояние и есть it-65-корпус (проверка счётчиков 57366/2690/4915, фоны 720/90/90).
2. **Уточнение протокола ДО прогонов (2026-09-21, dry-run QA):** первый вариант keep-списка
   (`Random(1337).sample(900, 300)`) выявил структурную корреляцию с `_split_groups` (тот же сид 1337):
   все 180 val/test-фонов попали в keep, train-фоны упали до 120 — нестратифицированная доля.
   Исправлено на стратифицированный прунинг независимым сидом: keep-списки режутся отдельно
   в каждом сплите, `random.Random(20260921)`: train 720→240, val 90→30, test 90→30
   (ожидается train-доля ≈ 0,42 %, val ≈ 1,1 %, test ≈ 0,6 %). Метрик it-70 ещё не видели —
   правка предрегистрации, не результатов.
   Скрипт-дроп `research/it70_prune_negatives.py` (по исходным stems `coco_bg_*` из prepared-имён
   `coco-background_{orig}_{i:07d}_{orig}`); keep-list — артефакт `research/it70_neg_keep.txt`
   (в манифесте, sha фиксируется). Исходники `train/data/coco-background/val2017` не трогаем;
   полный корпус восстанавливается командой prepare-visual it-65.
3. Тренировка — команда it-65 дословно, кроме `--name uav-yolov8s-bg70`
   (epochs 30, patience 10, batch 8, workers 2, base yolov8s.pt, imgsz по умолчанию).

## Протокол подтверждения (предрегистрируется СЕЙЧАС, до метрик it-70)

Вердикт-машинка `research/it70_verdict.py` (параметризованная копия it65_verdict; метка `uav-yolov8s-bg70`).

| крит | проверка | порог | база/источник |
|---|---|---|---|
| E1 | FP на НЕЗАВИСИМЫХ 400 фонах COCO val2017 (набор V1 it-69, байт-хеш-исключённый), новые веса it-70 | @0,25 ≤ 25,0 % **и** @0,5 ≤ 12,8 % | old 61,8/27,5; it-65-new 17,8/6,5 (V1) |
| E2 | mAP50 DUT test600 | > 0,720 | it-65 E2 (старые веса = 0,720) |
| E3 | mAP50 HF test600 | ≥ 0,837 | it-65 E3 |
| E4 | стоящий дрон session probe @0,5 | ≥ 16/18 | it-65 E4 |
| E5 | SAHI MMAUD recall полёт @0,5 | ≥ 89,5 % | it-65 E5 (там красный 84,3 % — целевая переменная рычага) |
| E6 | контур песочницы (урок V3 — уровень окон, боевой гейт 0,25): video-only recall airborne-окон | ≥ 0,98 | канон it-66 |
| E7 | контур: F1 fusion (late, τ=0,5) | ≥ 0,95 | канон it-66 0,961 |
| E8 | контур: медиана задержки обнаружения | ≤ старых весов + 1,0 с | канон it-66; presence-FP на стоящем дроне ошибкой НЕ засчитывается (урок it-66) |

- E1–E5 — образцы прогонов it-65 (те же скрипты/пороги, где применимо; E1 — вместо 90-кадрового
  теста it-65 берём независимые 400 V1, как и обещано уроком независимости).
- E6–E8 — прогон инференса новыми весами по песочным кадрам (imgsz 480, тот же протокол,
  что `yolo_sandbox_frames_new.csv`), далее 5 fusion-скриптов с `--suffix=-bg70` на гейте 0,25
  (фильтр max_conf<0,25→0 на уровне предфильтра, как в V3 it-69; на этом материале гейт 0,25
  не no-op в отличие от 0,4 — проверим и доложим честно).
- Одиночная семка тренировки — ограничение фиксируем в отчёте (стоимость ~сутки GPU; практика it-65).

## Правило вердикта (предрегистрировано)

Все E1–E8 зелёные → экспорт пары (чекпойнт it-70 + гейт 0,25 без изменений) разрешён:
`uavtrain evaluate/export`, фиксация sha, `models/registry.csv`, `models/README`, замена
боевых весов только после одобрения автора. ЛЮБОЙ красный → веса остаются майские,
в отчёте — честная констатация (вариант A исчерпан → бэклог B/C).

## Порядок работ

1. Прунинг + keep-list + счётчики в лог. (CPU, минуты)
2. Тренировка `uav-yolov8s-bg70` на GPU, resume-практика it-65; ожидание — условный монитор, не idle-опрос.
3. Замеры E1–E5 (по образцу it-65: eval-прогоны dut600/hf600, coco_bg_fp_eval на V1-400, session probe, MMAUD SAHI).
4. E6–E8 (sandbox-инференс + fusion-скрипты, `-bg70`).
5. Вердикт-машинка, отчёт, INDEX, манифест, коммит.

## Плечо S (добавлено по просьбе автора 21.09, после предрегистрации E1–E8): готовые модели вместо обучения

Автор: «может, просто посерчить модели именно вместо обучения — очень долго по дню делать».
Плечо регистрируется здесь как **скрининговое**, не вердиктное: обучать не нужно, но финальным
арбитром остаётся независимая линейка E1/E5 (см. оговорки).

- 7 HF-чекпойнтов (`research/shelf_models/`, вне git; sha256 первые 16 хех-цифр:
  doguilmak_v11x `8762f0759ddab1cd`, doguilmak_v8x `5953cb6c00b6f0e2`, skyguard_v11
  `33f5bc2309b61427`, ruju_v12 `3257a0bac00fd1d1`, danivelikova_v26n `60f5a2a5b0f963ba`,
  iris_v8s `412675eb07237a7b`, noah_v8s `bb06acbd90b5148f`): doguilmak v11x/v8x,
  SkyGuard v11, ruju v12 (AGPL), danivelikova v26n, IRIS v8s (CC-BY-NC), Noah v8s — все single-class id 0.
- `research/shelf_screen.py` (CPU, инкрементальный CSV `shelf_screen_results.csv`): mAP50 DUT-600 и
  HF-600 (split=test, imgsz640) + FP на 400 независимых фонах coco-bg-v1; первым — калибровочный
  прогон боевого майского веса (ожидаем 0,720 / 0,867 / 27,5 %).
- Оговорки (предрегистрируем ограничения): (1) shelf-модели вероятно учились на DUT/VisDrone-семействе
  → DUT600/HF600 для них могут быть in-domain (риск leakage) — поэтому вердикт по плечу S только после
  MMAUD distant-проверки (E5-линейка на CPU по 1–2 лидерам скрининга); (2) AGPL/CC-BY-NC лицензии —
  внутреннее сравнение допустимо, публичная поставка — нет.
- Сверка карточек HF (21.09, WebFetch — facts вместо догадок): **doguilmak v11x** — свой закрытый
  датасет (~1000 train / 350 val, single-class «drone», аугментации mosaic/HSV, imgsz640),
  заявлено mAP50 0,905 на СВОЁМ val, MIT; т.е. VisDrone/DUT не декларируются — прямой leakage
  маловероятен, но пересечений состава исключить нельзя. **SkyGuard yolov11** — MIT, карточка
  пустая (происхождение данных неизвестно — strongest caveat). **Noah Drone_Detection** —
  лицензия фактически **Apache-2.0** (не «неизвестна»), README пустой. **ruju v12** — карточка
  пуста, датасет не указан (в имени «-full-100m»), лицензия AGPL-3.0. Вывод: происхождение обучающих
  данных у 6 из 7 не верифицируется → арбитр только независимый MMAUD-SAHI, E2/E3 для полки —
  ориентир, не доказательство.
- Обучение it-70 НЕ остановлено: оно автономно (цепочка-вотчер сама считает E1–E8), и плечо S его
  не заменяет, а страхует: если полочная модель доминирует и на независимой MMAUD-линейке,
  рычаг рецепта может не понадобиться.
  **[Уточнение 21.09 14:29 — ниже по журналу: тренировка ПРИОСТАНОВЛЕНА по решению автора
  (GPU нужен для учёбы); с этого момента плечо S — активное и единственное, до отмашки.]**

## Ход работ

- 2026-09-21 14:13 МСК — прунинг применён: фоны train 240/56886 (0,42 %), val 30/2630 (1,14 %),
  test 30/4855 (0,62 %); labels-целостность OK; keep-list `it70_neg_keep.txt`
  sha256 `9a8b94089eb6…`.
- 14:17 — тренировка `uav-yolov8s-bg70` запущена отсоединённо (setsid+nohup; лог
  `train/runs/visual/it70_train.out`), ~2,4 it/s, 7111 батчей/эпоха → оценка ~50 мин/эпоха,
  ≤ 24 ч на 30 эпох (или раньше по patience=10). Инцидент запуска: первая попытка через
  управляемую фоновую задачу убита при перепривязке к таймауту инструмента (~2 мин 1-й эпохи
  потеряны, каталог прогона пересоздан); повтор — полностью detached.
- 14:20 — измерительная цепочка `research/it70_measure_run.sh` (watcher: ждёт «Results saved»,
  затем E2→E3→E1→E5→sandbox-кадры→E4→гейт-предфильтр 0,25→5 fusion-скриптов→манифест→вердикт;
  лог `train/runs/visual/uav-yolov8s-bg70/it70_measure.log`). Вердикт-машинка:
  `research/it70_verdict.py` (E1–E8, вход — артефакты с метками it70v1/bg70).
- Ожидание — условным монитором (финальный маркер лога), без холостых опросов.
- 21.09 14:29 МСК — **тренировка ПРИОСТАНОВЛЕНА по решению автора** («нужна будет карточка
  для учёбы… отложить, запастись оперативкой и терпением»): убита на середине эпохи 1/30
  (results.csv не создан — потерь артефактов нет, ~40 мин эпохи сгорели), вотчер цепочки
  замеров снят, монитор — снят, GPU освобождён (9 МиБ). Возобновление — команда `REPRODUCE §6c`
  с нуля, только после отмашки автора; прунинг корпуса остаётся применённым (обратим: keep-list
  + prepare-visual it-65). Активное плечо сейчас — S (shelf-скрининг, CPU ~0,75 ГБ RAM).

- 21.09 ~14:39 МСК — плечо S в процессе: калибровочная строка боевого майского веса в
  `shelf_screen_results.csv` сошлась с канонами бит-в-бит (DUT600 0,7202 / HF600 0,8673 /
  FP@0,5 27,5 % / медиана 0,333); идёт модель 2/7 (doguilmak v11x), темп ~25–30 мин/модель
  на CPU → ориентировочное завершение скрининга ~17:30–18:00 МСК. Реестр манифеста расширен
  артефактами it-70 (обоих плеч) — коммит 2c9f32a. After-screening: `it70_shelf_mmaud.sh`
  (MMAUD-арбитраж топ-3 на CPU, ~3–5 ч/модель).
- 21.09 ~14:49 МСК — подготовлен полный CPU-протокол для финалиста плеча S:
  `research/it70_shelf_fullprotocol.sh <pt> <prefix>` (E1 per-image, E4, sandbox-кадры,
  гейт-предфильтр 0,25, 5 fusion-скриптов, вердикт `--prefix <prefix> --e1 coco_bg_fp_<prefix>.csv`).
  Вердикт-машинка параметризована (142a822) и кросс-проверена на канонах it-65/66: на new-весах
  E6 (video-only R 0,946) и E7 (late F1 0,946) по критериям it-70 — КРАСНЫЕ (пороги 0,98/0,95);
  т.е. контурные E6–E8 — самостоятельный барьер для полки, а не формальность. Проверка гейта 0,25
  на new-sandbox: 0/146 обнулённых строк — для весов со сдвинутым вверх распределением предфильтр
  может быть no-op (урок V3 учитывается, это верхняя проверка).
- 21.09 ~14:52 МСК — первая полочная точка НЕ прошла скрининг: doguilmak v11x → DUT600 0,290 /
  HF600 0,527 / FP@0,5 34,7 % (хуже майского канона 0,720/0,867/27,5 %) — заявленный 0,905 mAP50
  был на собственном val автора модели, на наших линейках не подтвердился (эмпирическое
  подтверждение, почему E2/E3 полки без MMAUD-арбитра ничего не значат). Авто-арбитр поставлен в
  очередь: detached-вотчер PID 387917 ждёт «SHELF SCREEN DONE» и сам запускает
  `it70_shelf_mmaud.sh` (лог-триггер `research/shelf_mmaud_trigger.out`).
- 21.09 ~14:55 МСК — анти-баг проверка провала doguilmak v11x (правило «слишком плохо — тоже
  подозрительно»): spot-check на заведомо-позитивных sandbox-кадрах f_030/f_036/f_050/f_053 —
  имена классов `{0: drone}`, детекции есть, maxconf 0,37–0,70 @imgsz640 → загрузка/контракт
  корректны, разрыв 0,905 (карточка, свой val) → 0,290 (наш DUT600) = честный домен-сдвиг.
  Урок внесён в LESSONS скилла ai-research.
- 21.09 ~14:57 МСК — конвейер плеча S замкнут до финалистов: `research/it70_shelf_finalists.sh`
  (detached PID 389512) ждёт маркер арбитра, считает E5 по всем shelf-выходам и для каждого
  лидера с SAHI-полёт recall ≥ 0,895 (предрег. порог) автозапускает полный протокол E1–E8
  (`it70_shelf_fullprotocol.sh`, CPU; идемпотентно по «ЗАВЕРШЁН» в per-label логе). Отсечение
  по E5 до контура — соответствует протоколу («только финалисты»).
- 21.09 ~15:00 МСК — микрорезультат без инференса (`research/mmaud_recall_by_distance.py`,
  выход в `it70_recall_by_distance.txt`): провал E5 новых весов — ЧИСТО дальнобойный эффект.
  Recall полёта по дистанции (лидарный GT): старые веса 0–9 м 98,7 % / 10–19 м 89,7 %;
  новые — 97,5 % / **69,5 %** (−20,2 п.п. на дальних против −1,2 на ближних). Union со
  full@1920 у старых почти не добавляет (91,5 % на дальних) — SAHI 640 уже у потолка.
  Следствие для отчёта: «сдвиг conf-распределения на −0,1» (it-69) механизирован — он бьёт по
  мелким дальним целям; рычаг разбавления негативов (it-70) должен измеря той же линейкой,
  и полочных арбитров позже тоже стоит резать по дистанции.
- 21.09 ~15:01 МСК — из того же разложения: диагностический под-порог (НЕ новый критерий;
  вердикт остаётся агрегатным E5 ≥ 89,5 %): при ближних 0–9 м как у new (97,5 %) дальним
  10–19 м нужно ≥ 80,4 % (сейчас new 69,5 %, old 89,7 %). Любой кандидат (bg70 или полка),
  дающий E5-зелёный, обязан закрыть именно этот бакет — на него смотрим первым при красных.
- 21.09 ~15:08 МСК — pre-check автономной цепочки (пока скрининг на 3/7):
  (1) sha256 всех 7 полочных весов совпали с зафиксированными выше; case-маппинги label→файл
  в `it70_shelf_mmaud.sh` и `it70_shelf_finalists.sh` идентичны, путь `research/shelf_models/`
  резолвится в обоих — ночной запуск на этом не споткнётся;
  (2) класс-аудит: все 7 чекпойнтов одноклассовые (`{0: drone}`, у noah — имя «drones»), а
  `mmaud_sahi_full.py`/скрипты протокола берут max conf без фильтра классов → для этих весов
  это строго правильно (загрязнения person/bird нет); заодно подтверждено, что yolo12 (ruju) и
  yolo26n (danivelikova) грузятся в нашем ultralytics 8.4.139 на CPU.
- 21.09 ~15:12 МСК — найден и закрыт латентный баг идемпотенции арбитра: `mmaud_sahi_full.py`
  писал CSV построчно, поэтому килл по памяти оставлял бы частичный файл, который вотчеры
  (`[ -f "$OUT" ]`) приняли бы за полный замер. Теперь запись идёт в `*.tmp` с атомарным
  `os.replace` в конце — «файл есть» ⇔ «замер полон». Скрипт в момент правки не выполнялся,
  цепочку не задели; скринер не требует аналога (дописывает целую строку на модель).
- 21.09 ~15:14 МСК — аудит интерфейсов полного протокола плеча S: `--name` даёт
  `coco_bg_fp_<label>.csv` / `session_vis_probe_<label>.csv` ровно в записи вердикт-машинки;
  все пять fusion-скриптов несут `--yolo-csv`+`--suffix` (выход `event_metrics-<label>.csv`
  и т.п. совпадают с `--prefix`). Расхождений не найдено.
- 21.09 ~15:17 МСК — промежуточные числа скрининга (3/7 готовы): v8x финально
  0,2774/0,5275/FP@0,5 0,247 (гейт FP прошёл, суммарный счёт низкий);
  **skyguard-v11: 0,6592/0,8725/FP@0,5 0,168 — лидер полки** (MIT, 5,5 МиБ, карточка пустая —
  датасет неверифицирован, вероятен VisDrone-leakage: DUT600 близко к our-new 0,906? нет, ниже;
  HF600 0,873 выше нашего old-канона 0,867). Арбитр MMAUD примет top-3 — skyguard и v8x
  пока кандидаты.
- 21.09 ~15:31 МСК — анти-баг разбор ruju-v12 (0,0004/0,0000/FP 0,000 по правилу
  «слишком плохо — тоже подозрительно»): spot-check на 73 full-кадрах при imgsz640 — maxconf
  ≤ 0,011; в аргументах чекпойнта ЗАПИСАН imgsz=640, т.е. наш скрининг ровно в его родном
  контракте → конвейер чист. При 1024 модель «оживает» (f_050 0,34; f_030 0,28; всё < 0,5),
  при 1280 гаснет снова. Вывод: не баг, а годный лишь в чужом режимах слабый чекпойнт; под
  нашей фиксированной линейкой (640 для всех) дисквалифицируется честно. В лидеры не попадёт.
- 21.09 ~15:42 МСК — скрининг 6/7: danivelikova-v26n 0,006/0,066/FP 0,003; анти-баг
  spot-check: родной imgsz 640, maxconf на full-позитивах 0,13–0,14 (@640) / 0,27 (@1024) —
  живой, но слабый нано-чекпойнт, конвейер чист, дисквалификация честная. iris-v8s:
  0,2126/0,1900/FP@0,5 0,305 — провал гейта FP (домен EO/мультиспектральный, не удивляет).
  Расклад перед арбитром почти ясен: лидеры — skyguard-v11 (1,532) и doguilmak-v8x (0,805),
  третий — по итогам noah-v8s (последняя модель).
- 21.09 ~15:43 МСК — **SHELF SCREEN DONE (7/7)**. Вотчер 387917 штатно поднял арбитр:
  LEADER = skyguard-v11 (1,532), noah-v8s (0,838; FP@0,5 7,2 % — лучший на полке),
  doguilmak-v8x (0,805). Не прошли FP-гейт/счёт: v11x, iris (0,305), ruju (мёртвый),
  danivelikova (слабый). 15:43 старт MMAUD SAHI для skyguard (250/5091 за 3 мин → ЭВА ~1 ч,
  затем noah, v8x). Атомарная запись уже видна в работе (`.csv.tmp` в git status — partial
  наружу не торчит, как задумано).
- 21.09 ~16:33 МСК — **арбитр #1: skyguard-v11 SAHI-полёт = 90,9 % ≥ 89,5 % (E5-зелёный)**.
  Дистанционный разрез: 0–9 м 97,9 %, 10–19 м **83,0 %** — закрывает и диагностический
  под-порог дальнего бакета 80,4 % (у new — 69,5 %). Т.е. маленькая (5,5 МиБ) полочная модель
  на НЕЗАВИСИМОМ MMAUD держит дальние цели лучше, чем наши переобученные веса, и ближе к
  боевому канону (94,5 %/89,7 %), чем он к new. noah-v8s в прогоне с 16:33 (~1 ч), затем v8x.
  Неясное: датасет skyguard в пустой карточке — если его учили на VisDrone, то DUT/HF-цифры
  in-domain, но MMAUD-реколл от leakage не спасает только при честном домен-сдвиге — решит
  полный протокол E1–E8 (у вотчера финалистов skyguard уже в списке на автозапуск).
- 21.09 ~16:58 МСК — переэкзаменовка таймингов: noah-v8s идёт 250 кадров/24 мин (~9× медленнее
  skyguard — реальный v8s против 5,5 МиБ микро-веса) → ЭВА noah ~22:00, doguilmak-v8x (136 МиБ)
  ещё ~15–20 ч. Чтобы не держать небоеспособный результат за хвостом очереди, полный протокол
  E1–E8 для уже зелёного по E5 skyguard запущен параллельно (detached, лог
  `shelf_fullprotocol_skyguard-v11.log`, sha в шапке 33f5bc23… совпал) — честная ПЕРЕСТАНОВКА
  порядка, не изменение критериев; финалист позже пропустит его по маркеру «ЗАВЕРШЁН».
  Монитор `bbjl75j4g` следит за вердиктом/крахом.
- 21.09 ~17:17 МСК — **полный протокол skyguard-v11 завершён: ЭКСПОРТ НЕЛЬЗЯ.** Зелёные:
  E4 18/18, E5 90,9 % (первый полочный E5-OK), E7 0,954, E8 0,0 с. Красные: E1 FP@0,25 33,8 %
  /@0,5 16,8 % против ≤25/≤12,8 (тот же 0,168, что в скрининге — линейки сошлись, конвейер
  честный); E6 video-only recall окон 0,856 < 0,98 (стояночный дрон в контурных окнах);
  E2 по числу скрининга 0,659 < 0,720 (в вердикте ЖДЁТ из-за несовпадения имён eval-каталогов —
  переносим вручную, это не новый замер). Итог по cand: модель «видит дальних» лучше наших, но
  сыпется на FP-пороге и контурном реколле — майский боевой вес остаётся чемпионом.
- 21.09 ~17:19 МСК — health-check noah: прогресс подтверждён ростом `.csv.tmp` (буфер ~590
  кадров/46 мин → ЭВА ~23:40 МСК, в зависании не уличён).
- 21.09 ~17:50 МСК — **ОТМАШКА АВТОРА по GPU (для инференса)**: «я сказал гпу для инференса
  как раз для тебя, можешь юзать гпу для инференса, я про обучение моделей своих говорил».
  Тренировочное плечо T по-прежнему ПАУЗА. Действия: CPU-ветка арбитра (noah 500/5091 за 60+ мин)
  снята (PID 416866/403398/387917; мой partial `.tmp` удалён), `it70_shelf_mmaud.sh` и
  `it70_shelf_fullprotocol.sh` параметризованы `DEVICE` (default cpu = предрег. значение),
  арбитр перезапущен с DEVICE=cuda: skyguard пропущен по идемпотенности, noah на GPU даёт
  500 кадров/3 мин (~12× быстрее CPU) → ЭВА noah ~18:20, v8x ~19:30–20:00 МСК.
  Финалист-вотчер (389517) не тронут — его полный протокол пойдёт на cpu (наследует старый env),
  что корректно, но медленнее; при необходимости перезапустим с DEVICE после отмашки.
- Промежуточный факт скрининга: v8x dut600=0,277 (вторая doguilmak подтверждает домен-сдвиг
  семейства на DUT600; финальная строка CSV появится после её hf600+FP).

## Итог (скелет; заполняется по завершении плеч — до заполнения это НЕ вывод)

### Плечо T (обучение bg70) — ПРИОСТАНОВЛЕНО, не исполнено в этом цикле
- Статус на 21.09: приостановлено по решению автора (GPU под учёбу); возобновление — REPRODUCE §6c.
- Вердикт E1–E8: `research/.venv/bin/python research/it70_verdict.py; echo $?` → [заполнить].

### Плечо S (полка HF-моделей) — скрининг/арбитраж
| модель | DUT600 | HF600 | FP@0,5 | SAHI-полёт (E5-линейка) | дальн. 10–19м | вердикт-фильтр |
|---|---|---|---|---|---|---|
| old (канон) | 0,720 | 0,867 | 27,5 % | 94,5 % | 89,7 % | базовый боевой |
| new it-65 (канон) | 0,906 | 0,883 | 8,9 % | 84,3 % | 69,5 % | E5 красный |
| shelf-doguilmak-v11x | 0,289 | 0,527 | 34,7 % | — | — | отсев: FP-гейт |
| shelf-doguilmak-v8x | 0,277 | 0,528 | 24,7 % | арбитраж в очереди | — | гейт FP пройден |
| shelf-skyguard-v11 | 0,659 | 0,873 | 16,8 % | **90,9 %** | 83,0 % | вердикт: E1/E6/E2 красные → экспорт нельзя (E4/E5/E7/E8 зелёные) |
| shelf-ruju-v12 | 0,000 | 0,000 | 0,0 % | — | — | мёртвый вес (анти-баг: spot-check 0,011 в родном 640) |
| shelf-danivelikova-v26n | 0,006 | 0,066 | 0,3 % | — | — | слабый (анти-баг чист) |
| shelf-iris-v8s | 0,213 | 0,190 | 30,5 % | — | — | отсев: FP-гейт |
| shelf-noah-v8s | 0,205 | 0,634 | 7,2 % | в прогоне ~до 22:00 | — | FP лучший на полке |

- Пороги отсева: FP@0,5 < 27,5 % (скрининг), E5 ≥ 89,5 % (арбитр), под-порог дальнего бакета
  80,4 % (диагностика). Источники: `it70_recall_by_distance.txt`, `shelf_screen_results.csv`,
  `mmaud_sahi_full_<label>.csv`, `it70_shelf_report.py`.
- Лицензионный фильтр поставки: AGPL (ruju) / CC-BY-NC (iris) — только внутреннее сравнение.

### Вывод цикла (заполнить после вердиктов; предварительный каркас)
1. Рычаг негативов корпуса: [E5 у bg70] — гипотеза it-70 подтверждается/нет.
2. Полка: ноль/один/несколько кандидатов прошли независимый арбитр → [имена, лицензии].
3. Если оба плеча красные: аргументация в отчёт, что E5-потолок боевого майского веса (94,5 %)
   на данный момент не превзойдён ни рецептом корпуса, ни готовыми моделями; дистанционное
   разложение показывает, где именно искать (мелкие дальние цели, 10–19 м).
