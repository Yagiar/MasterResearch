# ИТЕРАЦИЯ 75 — Контур на pv-рядах ансамбля: OR-ряд держит тройку K1–K3 ЛУЧШЕ канона (R 1,000 / F1 0,965 / FP тот же набор окон)

- **Дата:** 2026-09-21. **Статус:** 🟢 ЗАВЕРШЕНА 21.09 (вердикт в «Результатах»; предрегистрация — коммит 2429db9).
- **Триггер:** it-74 закрыла ансамбль для контура только для одного ряда — AND (pv=min,
  красный G4: R 0,946). Симметричный **OR-ряд (pv=max)** не измерен ни в it-74 (её правило
  предрегистрировало только min), ни в it-71 (там политики над ОДНОЙ моделью). it-73
  предсказывает механизм: raising pv → рост FP на границах (насыщение), но это гипотеза,
  а не измерение.
- **Гипотеза:** найдётся pv-ряд ансамбля, который держит контурную тройку не хуже канона
  old (R 0,991 / F1 0,961 / FP 8) — т.е. ансамбль переносим и в контур, а не только в
  image-линейку.

## Ряды (pre-defined, ровно два — иных не добавлять)

- `AND` := min(c_old, c_new) — воспроизведение G4 it-74 (самопроверка: R 0,946 / F1 0,942 / FP 7);
- `OR` := max(c_old, c_new) — основная ячейка итерации.
- Каркас: it-71/it-74 (144 AST-окна, pa из `ast_windows.csv`, imgsz480 секундные ряды
  `yolo_sandbox_frames{,_new}.csv`, гейт 0,25 — боевой, политика D0 0,5·pv+0,5·pa ≥ 0,5).

## Предрегистрированные критерии (для OR-ряда; копия тройки G4)

- **K1:** R ≥ 0,98. **K2:** F1 ≥ 0,95. **K3:** FP ≤ 8.
- Сами-проверки до вердикта: канон old (0,961/0,991/FP 8), канон new (0,946/0,955/FP 7),
  AND-ряд it-74 (0,942/0,946/FP 7) — побитово с `it74_vote_analysis.txt`.
- **Правило вердикта:** K1∧K2∧K3 зелёные → «контурная заявка ансамбля пересматривается
  отдельно» (ряд с max-conf = тот же класс риска насыщения, что SAHI в it-73: точность
  на фонах меряется не контуром, поэтому зелёный K-триплет НЕ отменяет красный G2-риск —
  это регистрируется как гипотеза для живой проверки, не как результат).
  Любой красный → **ансамбль закрыт для контура**: вывод — контур остаётся на каноне old,
  ансамбль = исключительно image-линейка (и то с ×2-ценой доставки, решение автора).
- FP-окна OR-ряда выводятся списком (механизм: границы взлёта/посадки, как в it-73).

## Ограничения (заранее)

1. Тот же песочный материал n=1 сессия (урок it-72: независимого контурного материала нет) —
   это завершение сетки «ряд × каркас», не подтверждение.
2. OR-ряд в image-домене = OR@τ (it-74: FP до 49,8 %), т.е. зелёный контур при красных
   фонах не делает доставку привлекательнее — регистрируется механизм, не поставка.

## Скрипт

`research/it75_contour_vote.py` (выход `research/it75_contour_vote.txt`) — два ряда + вердикт.
Запуск после коммита предрегистрации.

## Результаты (21.09 ~21:38 МСК; все 3 сами-проверки рядов old/new/AND — OK)

| ряд | P | R | F1 | FP | FN | med задержка |
|---|---|---|---|---|---|---|
| old (канон) | 0,932 | 0,991 | 0,961 | 8 | 1 | 0,0 |
| new | 0,938 | 0,955 | 0,946 | 7 | 5 | 0,5 |
| AND(min) = G4 it-74 | 0,938 | 0,946 | 0,942 | 7 | 6 | 0,5 |
| **OR(max)** | 0,933 | **1,000** | **0,965** | **8** | **0** | 0,0 |

**ВЕРДИКТ: K1 ✅ (R 1,000) ∧ K2 ✅ (F1 0,965 > канона 0,961) ∧ K3 ✅ (FP 8 ≤ 8) — зелёные.**
FP-окна OR-ряда — **тот же самый набор из 8 окон, что у канона old**
([0,0; 8,0; 8,5; 10,0; 66,0; 66,5; 68,0; 68,5], прирост — пуст): pv растёт только там, где
доп-обнаружение second-модели не добирает до порога D0 на не-airborne-окнах (доп-FP-окон у OR
нет вообще), и ровно одно airborne-окно переходит в TP: t=58,0 — единственная потеря канона
(pv_old 0,692 + pa 0,274 → D0 0,483 < 0,5; OR-pv поднимает pv над порогом) → R=1,000.

1. По предрегистрированному правилу: **контурная заявка ансамбля пересматривается отдельно** —
   OR-ряд снимает главный контурный аргумент против ансамбля (AND-провал it-74 не обобщается
   на max-ряд). Это гипотеза для живой проверки (аналог it-68 с pv=max на второй модели),
   НЕ результат: материал n=1 сессия (урок it-72 — независимого контурного материала нет).
2. Механизм-оговорка (заранее, п.2 «Ограничений»): в **image-домене** OR@τ даёт красные фоны
   (it-74: FP до 49,8 % при τ=0,35) — контурная зелёность OR НЕ переносится на поставку presence
   по порогу; ансамбль в доставке остаётся двухрядным: image-линейка AND@0,40, контур OR-ряд.
   Стоимость та же: 2 инференса/кадр (решение автора).
3. Симметричный итог сетки «ряд × каркас»: min-ряд хорош против ложных срабатываний (image),
   плох на границах решения (контур); max-ряд зеркально. Оба конца исчерпаны — дальше только
   живые прогоны или рецепторные рычаги (плечо T, пауза автора).
