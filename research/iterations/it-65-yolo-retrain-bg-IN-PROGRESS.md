# ИТЕРАЦИЯ 65 — Переподготовка YOLO с фоновым корпусом (IN PROGRESS)

- **Дата:** 2026-09-06 — СТАТУС: тренировка запущена, идёт (~15-20 ч).
- **Источник:** it-64 аудит (0,002% негативов в визуальном корпусе); ревью §3.

## ТЕКУЩЕЕ СОСТОЯНИЕ (обновлено 2026-09-21 01:25 MSK / 22:25 UTC; прежде — ход 50, 20:40 MSK) — блок для перезапуска

Тренировка: 26/30 эпох записано (бест ep26: mAP50 0,83364 / mAP50-95 0,49638), с 00:07 MSK идёт
эпоха 27/30 (7171 итераций; темп после паники ~3 it/s → ~40–50 мин/эпоха), resume-процесс жив
(PID 5208, `it65_resume_train.py`), прогноз финиша ~01:00–04:00 UTC 21.09. Ранняя остановка не
сработает (best ep26 + patience 10 > 30) — доходятся все 30 эпох. Вотчеры-цепочки развёрнуты и сами
посчитают все замеры: `it65_chain.sh` (eval test → COCO-FP → SAHI MMAUD), `it65_chain2.sh`
(eval dut600/hf600 → session probe → манифест); логи — `train/runs/visual/uav-yolov8s-bg/it65_chain*.log`.
Монитор `it65_ready_monitor_v2.sh` активен (v1 заменён в 21:48). Память (срез 22:25 UTC): available 2895 МиБ;
swap — ОДИН файл `/swap.img` 4095 МиБ, занято 3533 (86 %), свободно 563 МиБ — риск OOM-убийства тренировки
при любом новом потребителе (политика «тяжёлыми не нагружать» покрывает); мониторинг — событие монитора
«ТРЕНИРОВКА-СТОП». Диск: 55 ГиБ свободно — артефактам цепочек хватит. Прежний срез 21:40: available 1735 МиБ — норм, но swap
вырос до 5961 МиБ (замер прежним способом; сегодня SwapTotal=4095 МиБ — значение выше общего объёма,
т.е. та цифра подозрительна на метод замера, перечитать её сейчас нельзя) — машину
тяжёлыми прогонами не нагружать, поднятие `uavdet-postgres` отложено до конца цепочек.

Содержательный шаг без нагрузки (21:40): в вольт заведена заметка
«Ложные срабатывания на фоне и негативные образцы» (Таксономия методов) — сюжет it-64→67
(FP старой модели 25,6 % на COCO-фонах, предрегистрированные критерии E1–E5, provenance it-67)
с перекрёстными ссылками на заметки «Проблема малого объекта (small target)» и
«Несопоставимость метрик и датасетов»; вольт вне git, коммита нет.

**Резервная копия цикла (ход 51, 21:57 UTC; обновлена 22:11, 22:27 UTC):** 123 локальных коммита (от it-30,
до `d51009e` включительно)
не отправлены в публичный remote — push вне рамок цели («коммит», не «push») и остаётся решением
пользователя; от потери диска закрыты инкрементальным бандлом
`backups/research-preit65-2026-09-21c.bundle`
(592 КиБ, `git bundle verify` OK, требуется ref `291c0ab`; коммит, фиксирующий эту запись, бандлом
не покрыт по построению — финальная пересборка на финализации; прежние `-2026-09-21.bundle` и `-21b`
остаются валидными предшественниками).

**Пре-флайт согласованности имён (ход 50, 21:41 UTC, без нагрузки):** сверил цепочки → вердикт →
манифест по всем артефактам — расхождений нет: `eval-visual --name {bg-new,new-dut600,new-hf600}`
пишет `train/runs/eval/visual-<name>/metrics.json` (шаблон `f"visual-{name}"` в `evaluate.py:50`),
что совпадает с путями в `it65_verdict.py` и ключами `make_manifest.py`; `coco_bg_fp_eval.py --name
new-yolov8s-bg` → `research/coco_bg_fp_new-yolov8s-bg.csv`, `session_vis_probe.py --name
new-yolov8s-bg` → `research/session_vis_probe_new-yolov8s-bg.csv`, SAHI `--out
research/mmaud_sahi_full_new.csv` — всё читается вердиктом по тем же путям. Синтаксис шага 5
(экспорт) подтверждён в CLI: `export-visual --weights --metrics [--out-name]`.

**Политика ожидания (правило пользователя 2026-09-20):** холостые опросы цели запрещены — ожидание
несёт условный монитор `research/it65_ready_monitor_v2.sh` (сменил v1 в 21:48; опрос раз в 10 мин,
печатает событие только при изменении состояния: новая эпоха / `шаг OK` / `шаг FAIL` /
`ВНИМАНИЕ-ЧАСТИЧНЫЙ` / `ВЕРДИКТ-ГОТОВ` и завершается). **v2 ужесточает готовность:** не «6 файлов на
диске» (SAHI/probe пишут CSV инкрементально — при FAIL шага остался бы частичный файл, и v1 дал бы
ложный ВЕРДИКТ-ГОТОВ с неполными E4/E5), а «финальные строки обеих цепочек + все 7 шаговых `OK`»;
при FAIL частичный файл помечается отдельным предупреждением «не читать».</strong> Агрессивные
idle-тики сжигают бюджет цели (100→128 за минуты), при этом решение всё равно невозможно до
появления артефактов цепочек.

Порядок действий после «цепочка 2 завершена» (все команды и заготовки в этом файле):
1. `research/.venv/bin/python research/it65_verdict.py` — вердикт E1–E5 (самотестирован, ходы 29–30;
   контрольный прогон 2026-09-21 21:56 UTC на пустом состоянии: корректный exit=1/ЖДЁТ по всем 5).
   **Не оборачивать в пайп** (`… | tail` теряет настоящий `$?` — покажет код `tail`): код возврата
   читать напрямую (`python research/it65_verdict.py; echo $?`), иначе зелёным можно ошибочно
   счесть ожидание и запустить необратимый экспорт.
2. Зелёный (exit 0) → экспорт по блоку «Порядок экспорта»: backup `cp` + сверка sha256 `41f3fd55…a262`
   → `uavtrain export-visual … --metrics …/visual-bg-new/metrics.json` → правка `models/README.md`
   (заготовка строки — там же) → перегенерировать манифест. Красный (exit 2) → НЕ экспортировать.
3. Финализация: снять IN-PROGRESS-маркер (переименовать файл → `it-65-yolo-retrain-bg.md`),
   заполнить колонку «Новая» в матрице, обновить INDEX (✅), коммит `research: it-65 — …`,
   затем удалить durable cron цикла `f271e0a9`.

## Выполнено до запуска тренировки

1. **DUT Anti-UAV скачан автономно** (gdown с Google Drive, БЕЗ капчи — публичные файлы): train 5200 / val 2600 / test 2200 кадров, VOC XML (класс UAV, 10109 боксов, негативов 3).
2. **COCO val2017 фоны**: images.cocodataset.org недоступен (connection reset); через HF `simopippa/reduced_coco_1000_val2017` (parquet) извлечено **900 фоновых изображений** без дрона (train 800 / val 100 / test 100).
3. **Парсер _parse_dut переписан**: явные сплиты из папок {train,val,test}/{img,xml}, размеры из <size> XML, кадры без <object> → негативы. Новый парсер `coco-background` (все изображения — негативы, каждый кадр — своя группа для случайного распределения по сплитам).
4. **Корпус пересобран** (`prepare-visual --datasets dut-anti-uav,hf-drone-detection,coco-background`): train 57366 (негативов 1,3%), val 2690 (3,3%), test 4915 (1,8%) — против 0,002% в старом.
5. **Аудит корпуса по доменам** (`corpus_stats.py`, 2026-09-20): train — HF 51446 (89,7%) / DUT 5200 / фоны 720; val — DUT 2600 / фоны 90 (HF-вал в новый корпус не вошёл); test — HF 2625 / DUT 2200 / фоны 90. Негативные кадры в test — только COCO-фоны (DUT test официальные сплиты негативов не содержат: 3 нег. в train, 0 в val/test). Масштаб целей: медианная площадь бокса DUT ≈ 0,011 доли кадра против HF ≈ 0,022 — цели DUT в ~2 раза мельче; при доминировании HF в обучении прирост на DUT будет консервативно занижен.

## Запущенная тренировка

- Отвязанный процесс (nohup, PID 1929404, переживает сессию): `train-visual --data _prepared/visual/data.yaml --base-weights yolov8s.pt --epochs 30 --patience 10 --batch 8 --workers 2 --name uav-yolov8s-bg`
- Лог: `/tmp/yolo_bg_train.log`; прогресс: `train/runs/visual/uav-yolov8s-bg/results.csv` (после паники 2026-09-20 — PID 5208, лог `train/runs/visual/uav-yolov8s-bg/resume.log`, см. «Инцидент»).
- Первый запуск (batch 16) упал по CUDA OOM (6 ГБ) → batch 8.
- Реальная скорость (замер): 1,1 it/s → **~1,8 ч/эпоха**; полные 30 эпох ≈ 54 ч, с early stopping реально ~25-30 ч. Процесс отвязан — переживает сессию; проверка прогресса: `wc -l train/runs/visual/uav-yolov8s-bg/results.csv` (строки = завершённые эпохи).

## По завершении (продолжение цикла)

> Автоматизировано двумя detached-очередями: `it65_chain.sh` → `train/runs/visual/uav-yolov8s-bg/it65_chain.log`
> (шаги 1–3), затем `it65_chain2.sh` → `it65_chain2.log` (шаг 1б подвыборки new-dut600/new-hf600,
> session-probe новыми весами, перегенерация манифеста). Ручные: экспорт (после просмотра метрик), live-прогон, отчёт.

### Предрегистрированный критерий экспорта (зафиксирован 2026-09-20, ДО просмотра метрик новой модели)

Экспорт в `models/visual/` + live-прогон — только если выполнены ВСЕ:

| # | Метрика | Порог | Baseline старой |
|---|---|---|---|
| E1 | FP на COCO-фонах test @τ=0.5 | ≤ 12,8% (двукратное снижение) | 25,6% |
| E2 | mAP50 на DUT-test600 (новый домен) | > 0,720 | 0,720 |
| E3 | mAP50 на HF-test600 (родной домен) | ≥ 0,837 (деградация ≤ 3 п.п.) | 0,867 |
| E4 | «Стоящий дрон» (негативная сессия, presence-TP) | ≥ 16/18 сек при τ=0,5 | 18/18 |
| E5 | SAHI MMAUD recall (полёт) | ≥ 89,5% (просадка ≤ 5 п.п. при подавлении FP) | 94,5% |

Любой красный — НЕ экспортируем; в отчёте — честная констатация какой критерий и чем обернулось
(дисбаланс корпуса 90% HF мог занизить E2 — трактовать по E1/E3/E4/E5). Отклонение от таблицы
допускается только с явной мотивировкой в отчёте.

Сверка автоматизирована: `research/it65_verdict.py` читает артефакты цепочек (COCO-FP,
metrics.json доменных eval'ов, session probe, SAHI-full с пересчётом сегментации по лидарному GT)
и печатает вердикт по таблице выше; пороги в скрипте — дословное повторение таблицы (добавлен
2026-09-20, после предрегистрации), ручного переноса цифр в финальное решение нет.
**Самоверификация вердикта (2026-09-20)**: функции `it65_verdict.py`, прогнанные по старым
каноническим артефактам, воспроизводят baselines точно — `fp_rate` = 25,556% (E1, base 25,6%),
`session_share` = 18/18 (E4), `sahi_flight_recall` = 94,520% (E5; сегментация полёт/стоит —
тот же bisect по лидарному GT, z>1 м, что в `mmaud_sahi_full.py`). Несопоставимости чисел
«вердикт vs baseline» в E1/E4/E5 нет; E2/E3 берутся из metrics.json тех же eval-прогонов.
**Сквозная сверка имён артефактов (2026-09-20)**: `it65_chain.sh` → `coco_bg_fp_new-yolov8s-bg.csv`,
`mmaud_sahi_full_new.csv`; `it65_chain2.sh` → `--name new-dut600/new-hf600`, а `eval-visual` добавляет
префикс `visual-` (доказано каталогами `visual-old-dut600/hf600`) → вердикт читает `visual-new-dut600/hf600`;
шаг 1 (`--name bg-new`) даёт `visual-bg-new/metrics.json` — путь аргумента `--metrics` команды экспорта.
Рассогласований, оставивших бы вердикт в «ЖДЁТ», нет.

1. `eval-visual` нового корпуса (test: 4915 кадров, 1,8% негативов) — сравнить mAP со старой моделью (baseline: по доменам — таблица «Подготовка»; старая на всём test не гонялась из-за объёма, сравнение по 600-кадровым подвыборкам HF/DUT: 0,867/0,720 mAP50).
2. Офлайн-оценка на MMAUD (`mmaud_sahi_full.py --weights <новые> --out research/mmaud_sahi_full_new.csv`) — сравнить SAHI-recall с 94,5% (it-63).
3. Замер FP на фонах COCO-test (`coco_bg_fp_eval.py --weights <новые> --name new`). Уточнение по «негативной сессии» из п.3 прежнего плана: она негативна только для цели «airborne» (дрон стоит в кадре; it-51), для цели «присутствие» — позитивна, поэтому визуальный FP-замер на пустых сценах корректно делает только COCO-фон; для сессии остаётся контроль, что новая модель не потеряла стоящий дрон (p_v на кадрах с visible=1, airborne=0 — ожидаемо TP по «presence»).
4. Если качество не упало, а FP на фонах снизился — экспорт в `models/visual/` и live-прогон.
   **Порядок экспорта (проверено 2026-09-20)**: перед перезаписью сохранить старую модель
   (`cp models/visual/yolov8s-uav.pt models/visual/yolov8s-uav-old-neg0.pt` — models/ вне git,
   перезапись необратима); live-прогону ничего не мешает: compose монтирует `models/:/models:ro`,
   pilot.yaml ждёт `/models/visual/yolov8s-uav.pt`, gpu-override правит только device — ребилд не нужен.
   **Контроль перезаписи (зарегистрирован 2026-09-20 до экспорта)**: sha256 текущих весов
   `yolov8s-uav.pt` = `41f3fd5593f41f6c18b13316ca48ac05e7a88ed14d4804428fb92035f8cda262` (6 215 274 Б,
   от 12.05) — после `cp` в `yolov8s-uav-old-neg0.pt` хэш backup обязан совпасть, а основной файл — измениться.
   Команда экспорта (код сверен 2026-09-20, повторно 21:52 и 22:12; `uavtrain/export.py`): `MasterDiploma/venv/bin/uavtrain
   export-visual --weights train/runs/visual/uav-yolov8s-bg/weights/best.pt
   --metrics train/runs/eval/visual-bg-new/metrics.json
   --dataset "hf-drone-detection + DUT Anti-UAV + COCO-фоны (негативы 1,3 % train / 3,3 % val / 1,8 % test), 1 класс drone, it-65, seed 1337"` —
   копирует (`shutil.copy2`, простой .pt,
   без ONNX) в `models/visual/yolov8s-uav.pt` (молча перезаписывает — отсюда backup выше);
   в **README ничего не дописывает**: append-only реестр ведётся в `models/registry.csv`, а md-строка
   только печатается в stdout — её вставлять в `models/README.md` вручную (README курируется руками);
   --out-name не нужен (дефолт — нужное имя). Прежний минус CLI (зашитый датасет «hf-drone-detection…»)
   **закрыт патчем 2026-09-21 22:12**: у `export-visual` появился `--dataset` (`cli.py`, smoke: `--help`),
   строка реестра теперь честная про корпус it-65 — ручной правки `registry.csv` не требуется.
   **Заготовка md-строки для реестра (ход 36, 2026-09-20)** — заменить датасет-и-метрики части
   сгенерированной экспортом строки; числа метрик подставить из `visual-bg-new/metrics.json`:
   `| YOLOv8s → UAV-bg (visual) | visual/yolov8s-uav.pt | hf-drone-detection + DUT Anti-UAV + COCO-фоны
   (негативы 1,3 % train / 3,3 % val / 1,8 % test), 1 класс drone, imgsz 640, ≤30 эпох (patience 10),
   seed 1337, дообучение от yolov8s.pt | **test**: … | 2026-09-XX |`;
   в старой строке реестра (2026-05-12) файл переименовать в `visual/yolov8s-uav-old-neg0.pt` —
   путь после перезаписи будет принадлежать новой модели.
   **Техническое ограничение (сверено 2026-09-21):** работающие `it65_chain.sh`/`it65_chain2.sh`
   нельзя править на месте — bash читает скрипт инкрементально, серединой файла попадёт мусор
   (внесённые правки уйдут в мутантный код). Поэтому контаминацию `conf_full1920/conf_union`
   в `mmaud_sahi_full_new.csv` (вызов без `--full1920-csv none`) править прогоном нельзя — только
   пост-обработкой: эти две колонки в отчёте не используются, вердикт читает чистый `conf_sahi`;
   «union новыми весами» остаётся открытым шагом (отдельный full@1920-прогон, вне it-65).
5. Отчёт it-65 финальный + INDEX + коммит весов (models/ не в git — только метрики).

   **Инвентарь документов, привязывающих майские числа к имени `yolov8s-uav.pt`**
   (обход 2026-09-21, grep по *.md; после экспорта имя файла будет принадлежать новой модели,
   исторические журнальные записи НЕ переписываем — к ним добавляется сноска-датирующая ремарка):
   1. `MasterDiploma/reports/runs_log.md` — строки 22, 68, 76, 106 (прогоны №1–№3: «test
      mAP@0.5=0.881…», «yolov8s-uav уверенно видит дрон») → одна сноска в шапке журнала:
      «`yolov8s-uav.pt` до YYYY-MM-DD — майский fine-tune (mAP@0.5=0,881), после — UAV-bg (it-65);
      прежние веса сохранены как `yolov8s-uav-old-neg0.pt`; записи №1–№4 относятся к майским весам»;
   2. `MasterDiploma/reports/pilot_results.md` — §1 строка 15 и §2 (таблица 0,881/0,906/0,813 +
      «Замечания») → та же ремарка под заголовком §2;
   3. `MasterDiploma/services/visual-detector/README.md:7` — «наша модель, обученная пайплайном
      train/» → дополнить корпусом (hf-drone + DUT + COCO-фоны) и версией (it-65);
   4. вольт `00 — Карта/03 — НИР-2/Каркас итогового отчёта…` и `Этап 6 — Пилотные исследования` —
      «готовые числа для вставки: test mAP@0.5=0.881» приписаны файлу весов → при переносе в главу
      диссертации числа прогона №4 атрибутировать майским весам (`-old-neg0.pt`), вольт правится
      в тот же день, что и runs_log (вне git);
   5. `research/iterations/it-04-yolo-frames.md:6` и `it-48-reproducible-kit.md:9` — исторические
      отчёты итераций, условия зафиксированы честно (по sha256 весов в манифесте it-48) — НЕ правятся.

## Подготовка (2026-09-20, пока тренировка идёт)

- **Базовый замер FP СТАРОЙ модели на COCO-фонах test выполнен** (`coco_bg_fp_eval.py`, CPU, 90 кадров, imgsz 640):

  | порог | кадров с FP (old yolov8s-uav) |
  |---|---|
  | 0.25 | 53 (58,9%) |
  | 0.40 | 30 (33,3%) |
  | 0.50 | 23 (25,6%) |
  | 0.70 | 4 (4,4%) |

  медиана max_conf = 0.284. Числовое подтверждение «всегда-положительного» видео (дефект it-64) — критерий для сравнения с новой моделью. CSV: `research/coco_bg_fp_old-yolov8s-uav.csv`.
- **Сверка парсинга вердикта (сухой прогон 2026-09-21, 22:09):** функции `it65_verdict.py` прогнаны на СТАРЫХ
  CSV: `fp_rate@0.5` = 25,556 %, `session_share@0.5` = 18/18, `sahi_flight_recall` = 94,520 %,
  `metric('visual-old-dut600'/'-hf600','map50')` = 0,7201 / 0,8673 — все пять парсеров
  (E1–E5) совпали с предрегистрированными baseline точь-в-точь; значит exit-коды вердикта после цепочек будут
  отражать только числа новой модели, а не ошибки разбора форматов.
- `mmaud_sahi_full.py` параметризован `--weights/--out` (по умолчанию — прежнее поведение).
- **Базовые замеры СТАРОЙ модели по доменам test** — детерминированные подвыборки 600 кадров/домен (`make_dut_test_subset.py`, seed=65, symlink-каталоги `visual-dut-test600`, `visual-hf-test600`), eval-visual на CPU, imgsz 640:

  | модель | домен | Images | Instances | P | R | mAP50 | mAP50-95 |
  |---|---|---|---|---|---|---|---|
  | old yolov8s-uav (обучена без DUT/фонов) | DUT-test600 (новый) | 600 | 614 | 0.857 | 0.675 | 0.720 | 0.387 |
  | old yolov8s-uav | HF-test600 (родной) | 600 | 660 | 0.897 | 0.790 | 0.867 | 0.414 |

  Артефакты: `train/runs/eval/visual-old-dut600`, `visual-old-hf600`. После завершения тренировки прогнать **обе подвыборки с новыми весами** (`--weights <new best.pt> --device 0 --name new-dut600 / new-hf600`) — прямое сопоставление «старая vs новая» по доменам одним протоколом; ждём деградацию ли на родном HF и выигрыш ли на DUT.
- **Оговорка об устройстве E2/E3 (сверка 2026-09-21):** базовые замеры старой модели делались на **CPU**, шаги 4a/4b chain2 — на **GPU** (`--device 0`). Для FP32-val в ultralytics различие устройств обычно пренебрежимо, но порог E2 жёсткий (0,720 против baseline 0,7201): **если mAP50 новой модели на DUT-test600 попадёт в [0,715; 0,725], повторить eval тех же весов `--device cpu` и трактовать E2 по CPU-значению** (та же машина, что и baseline). Протокол человеческий, цепочку не правим (живой скрипт).
- **Контроль «стоящий дрон» (негативная сессия, presence-позитив) — baseline старой модели**: `session_vis_probe.py`, CPU, 86 кадров (5 к/с) из `negative-session.mp4`, макс conf по секунде: **18/18 сек (100%) с детектом при любом пороге ≤0,7, conf 0,83–0,86**. Тест на то, что переобучение с фонами не потеряло наземного дрона; у старой модели тривиально проходит («всегда-положительная»). CSV: `research/session_vis_probe_old-yolov8s-uav.csv`.
- Состояние тренировки на момент подготовки: эпоха 23/30 идёт (~1,6 it/s), завершённые эпохи 21–22: P≈0.81–0.82, R≈0.94, mAP50≈0.93.
- **Пре-флайт проверки цепочки 2 (2026-09-20 21:13 UTC, до прогона)**: подвыборки на месте и целы —
  `visual-dut-test600` и `visual-hf-test600` по 600 изображений и 600 меток в `images/test`/`labels/test`,
  `data.yaml` с абсолютным `path:` и `test: images/test`. Шаг 4 не упадёт на отсутствующих данных.
  Шаг 5 (негативная сессия) тоже готов: `negative-session.mp4` на месте, его sha256 (1 397 645 Б)
  зафиксирован в `manifest.json` → замер old↔new идёт об одном и том же материале.
- **Найденный дефект сравнения (2026-09-20 21:27 UTC, до SAHI-шага): `mmaud_sahi_full.py` подмешивал
  в `conf_union` колонку `conf_full1920` из `mmaud_imgsz1920.csv`, а она измерена СТАРЫМИ весами (it-58).
  Прогон новыми весами дал бы «union» из детекций двух разных моделей — сравнивать old↔new по такому
  полю нельзя.** Исправление: флаг `--full1920-csv` (`none` — отключить мерж; по умолчанию — прежнее
  поведение it-63, чтобы старый результат оставался перепроизводимым). **Важно: запущенная цепочка 1
  вызовёт скрипт без флага**, поэтому в `mmaud_sahi_full_new.csv` поля `conf_full1920`/`conf_union`
  игнорируем как смесь моделей; вердикт E5 читает только `conf_sahi` — он чист. Для «union новыми
  весами» понадобится отдельный прогон full@1920 новыми весами (`--full1920-csv` на него) — не входит
  в it-65, отмечено как открытый шаг.
  **Аудит остальных измерителей цепочки на тот же класс дефекта (старая и новая модель в одном
  артефакте): `coco_bg_fp_eval.py` и `session_vis_probe.py` читают только сырые кадры (единственный
  `open()` — на запись), `eval-visual` — метрики своего прогона, `yolo_frames_eval.py` — кадры + GT.
  Стах-входов с предыдущими весами больше нет: SAHI был единственным случаем.**

## Инцидент (2026-09-20 18:45 MSK): kernel panic, потеряны ~13 мин эпохи 23

Машина ушла в kernel panic и перезагрузилась (сессия ZCode/Qoder прервалась). Восстановление:
`research/it65_resume_train.py` — `resume=True` от ultralytics с `weights/last.pt` (22/30 эпох
целы, results.csv не пострадал); лог — `train/runs/visual/uav-yolov8s-bg/resume.log`. Watcher
цепочки перезапущен с обновлённым паттерном процесса (`uavtrain.cli|it65_resume_train`) и логом
в каталоге прогона вместо /tmp (tmpfs теряется при перезагрузке). После паники скорость
3,1 it/s против 1,4 до (прежний темп, вероятно, съедало давление памяти: journald
«Under memory pressure» в 17:50). Следов OOPS/Xid/MCE в журнале перед обрывом нет; pstore пуст —
причину по дисковым логам установить не удалось (обрыв лога характерен для паники без флеша в journald).

**Наблюдение за памятью после резюма** (sampler `vitals_logger.sh`, шаг 5 мин): swap занят
1620→3245 MiB из 4095 за ~1,5 ч эпохи 23 при RAM available ≈4,3 GiB (ядро агрессивно выгружает
страницы под нагрузкой). Это не утечка процесса тренировки (RSS стабилен), а вытеснение кэша;
критично только при одновременном исчерпании RAM available и swap — порог тревоги: available
<800 MiB при swap >3900 MiB. RSS тренировки за 41 мин резюма ≈1,2 GiB (main 900 MiB + 2 воркера
по ~307 MiB) — роста нет, в swap уходят неактивные страницы посторонних процессов (на машине
крутится и valheim-сервер с RSS ~419 MiB; не трогаем). Предпосылка к повторению паники (давление
памяти до обрыва в 17:50) под наблюдением.

## Подготовка follow-up (it-66): пересчёт fusion-таблиц новыми весами

`yolo_frames_eval.py` параметризован `--weights/--out` (по умолчанию — прежнее поведение:
экспортированные веса → `yolo_sandbox_frames.csv`). Если экспорт E1–E5 состоится, sandbox-домен
нужно перескорить новой моделью и перегнать fusion-симуляции — это первый шаг it-66:

```bash
research/.venv/bin/python research/yolo_frames_eval.py \
  --weights MasterDiploma/models/visual/yolov8s-uav.pt \
  --out research/yolo_sandbox_frames_new.csv
# далее — fusion-контур принимает --yolo-csv и --suffix (output: <имя><suffix>.csv; значение
# с дефисом передавать только формой --suffix=-new): пересчёт =
#   fusion_sim_full / stress_sim / event_metrics / threshold_calibration_v2 / bootstrap_delta_v2
#   --yolo-csv research/yolo_sandbox_frames_new.csv --suffix=-new
# дефолтные артефакты it-36..41 не перезаписываются; смоук-прогон с --suffix дал побайтно
# идентичные каноническим таблицы (коммит подготовки it-66)
```

## Матрица сравнения (ход 38: baseline-колонки сверены с артефактами; колонка «новая» — из вердикта)

| Крит. | Метрика (источник числа) | Старая модель | Порог | Новая |
|---|---|---|---|---|
| E1 | FP на 90 COCO-фонах test @0,5 (`coco_bg_fp_*.csv`) | 25,556 % | ≤ 12,8 % | — |
| E2 | mAP50 DUT-test600 (`visual-{old,new}-dut600/metrics.json`) | 0,7201 | > 0,720 | — |
| E3 | mAP50 HF-test600 (`visual-{old,new}-hf600/metrics.json`) | 0,8673 | ≥ 0,837 | — |
| E4 | Секунды с детектом @0,5, «стоящий дрон» (`session_vis_probe_*.csv`) | 18/18 | ≥ 16/18 | — |
| E5 | SAHI recall полёт, MMAUD 5091 кадр (`mmaud_sahi_full*.csv`) | 94,52 % | ≥ 89,5 % | — |

Числа старой колонки — те же функции `it65_verdict.py` по каноническим файлам (самотест, ходы 29–30);
заполнение «Новой» — автоматическим прогоном вердикта после цепочек, без ручного переноса.

## Готовность live-прогона (проверено ходом 39, 2026-09-20)

Docker 29.4.3 + Compose v5.1.3, nvidia-container-cli на месте, цель `run-pipeline-gpu` в Makefile есть.
Пайплайн не поднят; в `docker ps` — только посторонние контейнеры `parsernedviga-*` (их не трогать и
не гасить общими командами). Ограничение по времени: live-прогон запускать только после завершения
цепочек оценки (GPU последовательно), экспорт к тому моменту уже должен лежать в `models/visual/`.
