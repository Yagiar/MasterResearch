# ИТЕРАЦИЯ 22 — Pandoc-рендер отчёта НИР-2: раздел 7 и источники [37]–[39] конвертируются корректно

- **Дата:** 2026-09-04
- **Цель:** проверить, что вставленный в it-21 раздел 7 рендерится pandoc'ом без дефектов (таблицы, источники, кириллица) — .docx в git не входит (по AGENTS генерируется, `reports/**/*.docx` в .gitignore).
- **Инструмент:** pandoc 3.9 (через `pypandoc-binary` в `research/.venv` — системного pandoc на машине нет; для GPU-машины достаточно `pandoc otchet.md -o otchet.docx`).
- **Команда:** `pypandoc.convert_file('reports/НИР-2/otchet.md', 'docx', outputfile='reports/НИР-2/otchet.docx', extra_args=['--standalone'])` → 44 603 байта, без ошибок.

## Проверка содержимого docx (распаковка word/document.xml)

10/10 контент-проверок пройдено: заголовок и подзаголовки 7.1; таблица 7.4 (значение 0,246); таблица 7.7 (0,913); формулировка «медиана p_a по 5 перекрывающимся окнам»; источники 37 (Pawełczyk), 38 (AST: Audio Spectrogram Transformer), 39 (DADS); упоминание MMAUD [35] в тексте; вывод 7.4 «ухудшает решение относительно одномодального». **Таблиц в документе: 8** — все markdown-таблицы раздела 7 (и прежних разделов) сконвертировались.

## Состояние git

`otchet.docx` подтверждённо игнорируется (`MasterDiploma/.gitignore:51 reports/**/*.docx`), в индекс не попал; в репо остаётся только markdown.

## Выводы

1. Раздел 7 готов к перегенерации .docx у автора — блокеров рендера нет; markdown-таблицы pandoc 3.9 съедает без настройки.
2. Замечание для финализации: pypandoc-путь — временное решение этой сессии; в AGENTS/Makefile команда остаётся `pandoc otchet.md -o otchet.docx` (на машине с pandoc).
3. Рендер-проверку стоит повторять после каждой правки раздела 7 (добавлена в чек-лист it-21 «для автора»).
