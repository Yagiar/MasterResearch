# ИТЕРАЦИЯ 39 — Фикс по ревью: воспроизводимость research-скриптов (§11, частично)

- **Дата:** 2026-09-05
- **Источник:** внешнее ревью GPT-6-Astra (`MasterResearch_review.md`, п. 11: «в исследовательских скриптах встречаются абсолютные локальные пути»).
- **Компоненты:** 9 скриптов `research/` (ast_windows_eval, score_stages, fusion_sim_full, stress_sim, threshold_calibration, score_fusion_vs_gt, channel_health_v2, channel_health_sim, yolo_frames_eval).

## Изменение

`ROOT = "/home/otrix/code/GeneralFolderMasterDiploma"` → `ROOT = str(Path(__file__).resolve().parent.parent)`: скрипты работают из любой копии репозитория (клонируема/переезжаема), корень вычисляется от расположения скрипта. `fusion_sim_full.py` перезапущен после правки — CSV детерминированно совпал.

## Что закрыто из п. 11, что осталось (зафиксировано как задачи)

- ✅ абсолютные пути убраны;
- ❌ самодостаточный воспроизводимый комплект (манифест split, контрольные суммы весов, идентификаторы ревизий моделей, команда пересчёта каждой таблицы) — P2, отдельная итерация после сбора независимых данных;
- ❌ универсальная `evaluate_fusion_jsonl` в train-контуре — P2 (оценка пока живёт в research-скриптах, что явно документировано).

## Проверка

Перезапуск `fusion_sim_full.py` из корня — идентичный CSV (19 строк). Полный монорепо-набор 58/58.
