# DECISION — роадмап после it-82 (по внешнему аудиту 24.09.2026)

Источник: развёрнутый аудит проекта, присланный автором 24.09.2026 («Глубокий аудит
MasterResearch и рекомендуемый план после it-82»). Полнотекст — в переписке сессии; здесь —
принятые решения и порядок работ. Статус: **принято** (автор: «лан по пушу отложи… выстрой
план»; план аудита взят за основу без правок).

> **Состояние на вечер 24.09 (справка; таблица ниже — плановая, не переписывается):**
> it-83 🟢, it-84 🟢, it-85 🟢 (потолок устойчивой частоты ≥5 к/с, цена ×2 = VRAM/util),
> it-86 🟡 SANITY-закрыта (headline M1–M3 — блок: запись автора 10–30 мин), it-87 🔄
> (протокол заморожен, обвязка + live pre-flight 🟢; боевое однократное открытие — блок:
> запись материала по спеке), it-88 и it-89 — закрыты **отрицательно** (SAHI-угол
> FP≤12,8∧recall≥85 не собран; стоп-критерий skyguard сработал на zero-inference анализе).
> Канон чисел — `INDEX.md`. Параллельные письменные обязательства исполнены: вечерний
> проход 24.09 внёс позиции 11–17 REPORT-DELTA в §7.7/7.8 отчёта НИР-2 (§8 — за строкой 18,
> ждёт боевого it-87).

## Главный тезис аудита (принят)

Цикл it-01…82 перевести из режима «ищём ещё метрику» в режим **заморозки и подтверждения**:
все it-01…82 объявляются development/exploratory evidence; далее — чинящие валидность работы
(media_ts, paired replay, throughput, мультимодальный негатив) и **один замороженный
confirmatory holdout** на материале, который цикл никогда не видел. После holdout — стоп
экспериментов (новые допустимы только для блокер/багов).

## Нумерация (INDEX-фактические номера; в скобках — условные имена аудита)

| № | работа | приоритет | состояние |
|---|---|---|---|
| it-83 | фикс `media_ts` режима папки (`requested_fps` вместо nominal 30) + pytest-регресс + короткий live-sanity A/B | P0 | 🟢 ЗАВЕРШЕНА 24.09 |
| it-84 | точный **paired** replay old vs AND@0,4 (один finite список frame-id, loop off, дренаж Kafka, гейт completeness, discordant-таблица McNemar/cluster-bootstrap) | P1 | 🟢 ЗАВЕРШЕНА 24.09 (P1 🟢) |
| it-85 | throughput saturation sweep (fps 0,5…5: coverage, lag, p50/p95/p99, GPU util/VRAM, drain-time; определение sustainable throughput предрегистрировано) | P1 | 🟢 ЗАВЕРШЕНА 24.09 (потолок ≥5 к/с) |
| it-86 | мультимодальный **синхронный** негатив V0/V1/V2 (video off-gate / +audio gate / AND+gate; при отсутствии своего синхронного материала — запись 10–30 мин cam+mic без дрона; telemetry-гейты покрытия аудио, уроки it-30) | P2 | 🟢 SANITY; full — шаг 5 чек-листа после it-87 |
| it-87 | frozen final holdout (предрег. протокол: веса/sha, τ, режим входа, fusion, метрики вкл. false-alarms/hour и TTD; открытие материала — однократно; красный результат не триггерит новый τ) | P1-финал | 🔄 ждёт запись автора (holdout-24) |
| it-88 | tile-aware SAHI aggregation (empirical-null percentile по n_tiles; cross-tile consistency; full-frame corroboration) — exploratory, затем только на holdout | P2 | 🔴 закрыта отрицательно 24.09 (T1) |
| it-89 | skyguard third voter — **только** после zero-inference disagreement analysis (rescued_TP/introduced_FP/rescue_precision на множестве old≠new); стоп-критерий: нет complementary signal → закрыть отрицательно без GPU-прогона | P3 | 🟢 закрыта отрицательно 24.09 без GPU (Q1/Q2) |

Порядок it-83 → (документация — этот же коммит) → it-84/85 → it-86 → it-87; письмённые
обязательства (§7.7/7.8 под it-82, §8 Заключение, глава про ансамбль) идут параллельно,
формулировки — по разделу ниже.

## Терминологические решения аудита (приняты, обязательны для отчёта/диссертации)

1. **Разыменование политик:** `OR_event_offline` (it-75, F1 0,961→0,965 — только offline
   event-протокол), `AND_presence_live` (it-81/82 — боевая гипотеза), `OR_presence_live`
   (опровергнутый контроль, 100 % FP в it-82). Формулу «контур=OR» без имени политики из
   текстов убрать — внешнего противоречия «OR рекомендован ↔ OR 100 % FP» комиссия не увидит.
2. **Operating point = f(режим инференса):** числа всегда с режимом входа; формула «модель
   имеет FP=7,2 %» запрещена, канон — «политика old⊕new CONF-AND@0,40: 7,2 % на 640/
   full-frame негативном протоколе; в SAHI-HD — 48,8 %; однородная HD-точка отсутствует».
3. **Target-Variable column:** в каждой итоговой таблице — столбец target
   (flight/E5 vs presence vs event/contour); 94,7 % и live R=1,000 не ставить в один ряд.
4. **Мультимодальность:** тезис «fusion лучше обеих модальностей» не утверждать (it-40/41:
   audio-only при своём пороге 0,978, превосходство слияния на пилоте не установлено);
   канон — комплементарность отказов + управляемая деградация через adaptive gating.
5. **Цена ×2:** до it-85 писать «медианная e2e-задержка в it-81 не отличалась; влияние на
   устойчивую входную частоту не измерено (при 2 к/с покрытие ~57 % — it-82)».
6. **Статистика it-82:** «ДИ не перекрываются» — не главное доказательство (234 vs 199
   кадров, автокорреляция); подтверждение несёт paired replay it-84 (discordant pairs,
   bootstrap по frame-id).

## Стоп-критерий (фиксирую заранее)

После закрытия it-87 дальнейшие эксперименты — только устранение блокер/багов. Улучшение
headline-метрик на development-наборах (MMAUD/E-линейка/OI/sandbox) запрещено как
adaptive reuse. Дальнейшие текстовые работы (главы, заключение, правки отчёта) — не эксперименты.

## Порядок действий при появлении материала (чанк-лист; писать до выполнения)

1. `MasterDiploma/sandboxDataForSimulator/holdout-24/manifest.tsv` появился → runner сам
   прогонит up-front валидацию (файлы, кавычки, pos≥5 ∧ neg≥600 с); отказ на этом шаге —
   НЕ боевой пуск (материал чинится, протокол не потрепан).
   1а. Состояние до пуска (проверено 24.09, повторять только если веса правились):
       sha хостовых `models/visual/yolov8s-uav.pt` = `41f3fd55…` и
       `uav-yolov8s-bg-best.pt` = `7042602a…` — канону H0 соответствуют; маунт
       `../models:/models:ro` (docker-compose.app.yml:55) — контейнер видит ровно эти файлы
       read-only, дрейф весов между проверкой и прогоном структурно исключён; путь
       `VOTE_WEIGHTS_PATH` в оверлее = тот же файл.
   1б. Боеспособность обвязки (сверено 24.09): GPU свободен (1660 Ti, 0 % утилизации,
       nvidia CDI виден в docker), overlay-имена трёх раннеров уникальны
       (it86/it86f/it87) и не затирают друг друга, `offset()` даёт 0 при отсутствующем
       decisions.jsonl (свежий диск не ломает нумерацию срезов); диск: 108 ГБ свободно,
       боевой прирост jsonl оценивается в ~30 МБ (десятки тысяч решений × ~400 Б) — запас
       двукратный с голо́вом. При появлении материала
       повторить только утилизацию GPU (кто-то мог занять).
2. `bash research/it87_holdout_run.sh` — ровно ОДИН раз (дефолт-песочница holdout-24;
   любые HOLD_SUBDIR/MIN_NEW overrides только для смока, не для боя).
3. Из `research/it87_holdout_run.log` достать строки `SCORE_OFF id START:END` → ОДИН разбор:
   `mapfile -t SRES < <(awk '$1=="SCORE_OFF"{print $2 "=" $3}' research/it87_holdout_run.log)`
   `research/.venv/bin/python research/it87_holdout_eval.py --manifest=<путь к holdout-24/manifest.tsv> "${SRES[@]}"`
   (путь = дефолт внутри eval, можно без флага; явный параметр — только если манифест переложен)
   (идиомы SRES/FRES сквозно проверены тестом лог→awk→eval 24.09; НЕ `SRES=$(…)`/`$SRES`
   без кавычек — bash не вырезает встроенные кавычки из подстановки, имена it-86-full с пробелом;
   повторный РАЗБОР допустим, повторный ПРОГОН — нет; каждый переразбор — ДО chmod 444 из п.3.5,
   иначе ОТКАЗ записи eval). Выход: `it87_holdout_summary.txt`, `it87_holdout.csv`.
3.5 Финальный разбор устроен → заморозить канон: `chmod 444 research/it87_holdout_summary.txt
   research/it87_holdout.csv` (существование summary — гейт it86-full; с 444 оно ещё и
   неизменным; переразбор после заморозки — только осознанным chmod u+w).
4. Закрытие it-87 по ритуалу (красный — тоже закрытие): отчёт-итерация, канон-блок INDEX,
   SYNTHESIS (если меняет claims), REPORT-DELTA позиция 18 (§8 Заключение) — фактический
   вердикт, тестовый контур, манифест. Конвенция имени (как it-84/85/86): git-rename
   `it-87-frozen-holdout-PLANNED.md` → `it-87-frozen-holdout.md` + синхронная правка 6
   держателей ссылки (`it87_holdout_eval.py`, `it87_holdout_run.sh`, `REPRODUCE.md`,
   `REPORT-DELTA.md`, `INDEX.md`, `sandboxDataForSimulator/README.md`; сверено git-грепом
   24.09); два из них — манифестные файлы → пересборка манифеста
   в ритуале покрывает.
5. ТОЛЬКО ПОСЛЕ шага 4 — it-86-full: `bash research/it86_full_negative_run.sh` (плечи V0/V1/V2
   на neg-сегментах holdout-24; runner сам требует завершённый разбор it-87 и негативы ≥600 с;
   разбор — `it86_sync_negative_eval.py` объединёнными `SCORE_OFF`-срезами плеч; eval аттестован
   под multi-segment синтетикой `it86_eval_selftest.py`: покрытие = union по сегментам,
   число сегментов в срезе сверить с манифестом); экстрактор имён с пробелом — та же mapfile-
   идиома (сквозной тест 24.09): `mapfile -t FRES < <(awk '$1=="SCORE_OFF" && $2 ~ /^V[012]$/ {print $2 " " $3 "=" $4}' research/it86_full_run.log)`,
   затем `it86_sync_negative_eval.py --out=research/it86_full_summary.txt "${FRES[@]}"` (--out
   обязателен: дефолт eval — SANITY-канон `it86_sync_summary.txt`, с 24.09 механически заморожен
   444 + ОТКАЗ записи, так что пропуск --out роняет разбор, а не затирает канон). Финальный
   разбор → `chmod 444 research/it86_full_summary.txt` (канон it-86-full). Срезы it-87
   не затрагиваются: decisions.jsonl append-only, разбор it-87 к этому моменту завершён.
6. Любой H0-блокатор на шаге 3 → стоп, честная фиксация «замер не состоялся + причина»;
   повторный прогон — только с явной письменной пометкой автора о первом пуске.

**Пре-открыточный статус (24.09):** механизм шагов 2–5 прошёл сквозную репетицию end-to-end
(заезд `baf01bkmb`: 3 плеча × 3 сегмента по 68 с, M0 ПРОЙДЕН, разбор с явным `--out` вне
манифеста; отчёт it-86 п.7, коммит 2a9c80d). Находка «watermark-релиз mono-V0 только
событийным max_wait» (отчёт it-86 п.6) на боевом материале ≥600 с не блокирует. Открытие
блокирует только запись автора (`holdout-24/manifest.tsv`; монитор PID 1542426).
Серия input-validation hardening pre-flight гейтов того же дня (после репетиции, до
открытия; предрегистрации не противоречит — инфраструктура отказа, а не протокол замера):
BOM/CRLF-устойчивость парсеров (15962f6) + отказ на `"`, `$`, backtick, `\` в именах файлов
(heredoc-оверрай раскрывает `$`/backtick молча; b2dda5d) + строгий числовой regex на
`dur/gt_start/gt_end` (запятая `95,0` в awk читается как 95, а eval крашится ПОСЛЕ прогона;
37f00ba) + ASCII-формат `segment_id` `[A-Za-z0-9._-]` (пробел ломает awk-идиому разбора
`SCORE_OFF`; 2d5d7cc) + уникальность `segment_id` (дубликат молча съедался бы словарём eval
после необратимого замера; живой тест в обоих раннерах). Каждый проверен живым пуском с dummy-манифестом (ОТКАЗ ДО docker/tee,
боевой лог не создаётся) и покрыт пересборкой манифеста. Дополнительно: `it86_sync_summary.txt`
заморожен chmod 444 + eval ОТКАЗЫВАЕТ при пропуске `--out` (случайный боевой разбор без
явного пути больше не затирает SANITY-канон; легитимный replay — в песочницу, REPRODUCE §6v).

## Что уже выполнено из P0-блока аудита (этот проход)

- Канон-блок состояния в шапке `INDEX.md` (заменил «Состояние цикла на 22.09 / 259 коммитов»).
- `REPRODUCE.md`: 111 → 118 файлов манифеста.
- `MasterDiploma/README.md`: убрано «Реального Docker-прогона пайплайна ещё не было»
  (противоречило it-30/42…46/81/82); синхронизирована ловушка в `AGENTS.md`.
- it-83: фикс `media_ts` — см. `iterations/it-83-media-ts-fix.md` (тесты requested_fps,
  регресс pytest, live-sanity).

## Ждут решения автора (зафиксировано 24.09, чтобы не потерялось)

1. **Промоушен уроков `seen ×3`** из `~/.qoder/skills/ai-research/LESSONS.md` в Operating
   principles/`references/`: (а) иноязычные вставки в русские тексты (ловить сканом до коммита),
   (б) мета-урок «автоматический дедуп log_lesson.py — черновик, сверять вывод вручную».
2. **PRUNE LESSONS.md**: 248 строк при лимите ~150 (на 24.09) — какие entry сбрасывать, решает автор.
3. **Push** 76+ локальных коммитов в `origin/main` — отложен автором («я сам там»).
